```bash
npm install -g anypick
```

The binary installs as `anypick`. AnyPick needs
Node.js 22.5 or newer, because it stores its data in SQLite through
`node:sqlite`.

Later, `anypick update` upgrades an npm-installed copy in place.
