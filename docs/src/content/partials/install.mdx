```bash
npm install -g hotplug
```

The binary installs as `hotplug`; `rotate` is kept as an alias. Hotplug needs
Node.js 22.5 or newer, because it stores its data in SQLite through
`node:sqlite`.

Later, `hotplug update` upgrades an npm-installed copy in place.
