# Ink's react-reconciler must load the same React instance the app imports.
# Default pnpm isolation installs a second physical React under the reconciler
# package; hooks then throw "Invalid hook call" and every TUI screen is blank.
node-linker=hoisted
