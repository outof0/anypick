/** Stable adapter contracts for programmatic extensions. */
export type {
  CatalogProvider,
  ClientAdapter,
  ClientCapabilities,
  PluginContext,
  HotplugPlugin,
  Provider,
  Protocol,
  SourceAdapter,
  SourceCapabilities,
  TransportCapability,
} from './types';
export { PLUGIN_API_VERSION } from './types';
